# EduFlow Platform Quality Sweep — BMad Method Master Tracker

**Started:** 2026-05-15
**Goal:** Enterprise-grade quality across the entire platform via full BMad ceremony applied part-by-part.
**Method:** For each part — `document-project` → `create-architecture` (if needed) → `create-epics-and-stories` → `check-implementation-readiness` → per-story `CS → DS → QA → CR` → `retrospective` → adversarial review.

## Sequence

User-confirmed order: **3 → 1 → 2 → 4 → 5 → 6 → 7 → 8 → 9 → ...**

| # | Part | Status | Sub-stories | Started | Closed | Notes |
|---|---|---|---|---|---|---|
| 0 | **Hotfixes** | ✅ done | 3 P0 production failures | 2026-05-15 | 2026-05-15 | Closed before Part 5: GET /uploads/serve/{filename} now requires auth and school-scoped lookup; receipt download path is guarded and backend receipt endpoint is covered by regression test; staff leave approval now uses require_owner_or_principal. |
| 0.5 | **Pre-Part-9 Infra** | ✅ done | 3/3 stories | 2026-05-16 | 2026-05-16 | 5 indexes (exam_results, audit_logs, lesson_plans, sms_logs TTL, notifications), factories.py (6 factories), unauthenticated surface test (caught+fixed 9 open routes in academics/settings/students). |
| 1 | **Auth + RBAC** | ✅ done | 12/12 original concerns closed + all 16 Part 1.5 findings closed | 2026-05-15 | 2026-05-15 | Part 1 hardening + Part 1.5 fix sweep complete. ~119 of ~130 inline role checks migrated to `Depends(require_role/require_owner/require_owner_or_principal)`; 11 remaining are documented composite/dynamic gates annotated with `# auth:`. Migration 016 idempotent; scope_resolver denies by default; designation fallback removed; cookie phantom evicted; concurrent-refresh test now deterministically races; scoped_query rejects cross-branch attempts. 260 backend tests passing (up from 224 baseline). See `parts/auth-rbac/review-findings-and-fix-plan.md` for the 16 patches A–P. |
| 2 | **AI Layer** | ✅ done | 11 of 11 patches landed (P1–P11) | 2026-05-15 | 2026-05-15 | Audit done (47 raw findings → 11 patches). Wave 1 (P1–P5 Critical) `e8b4a70`. Wave 2 (P6–P11 Important+Polish) `7e82b31`. 311 backend tests passing (was 260 at Part 1 start). P6: registry sub_categories + _is_tool_authorized single gate; P7: announcement moderation in tool (pending_approval for teacher/student audience); P8: content filter on rich_blocks + tool data for students; P9: confirm-token tenant binding (school_id/branch_id), peek raises on Mongo error, $gte TTL, compensating decrement; P10: expires_at required on overrides, stable sort, migration 017; P11: safe_token_count coerce, restricted_exact+9 keys, phone exact-match, random.uniform delay, SSE keepalive comment, zero-width whitespace, _json_candidates rich extraction, numeric param validators. |
| 3 | **Owner role (vertical)** | ✅ done | 12/12 extracted FRs verified + full cross-part review sweep | 2026-05-15 | 2026-05-15 | Part 3 closed. All FRs verified. Full 5-agent party-mode review (Parts 1+2+3) found 29 additional bugs — all fixed across commits 13388ae, 22c467d, e260247. Backend: 387 tests passing (0 skipped). project-context.md fully refreshed (34 patterns). Key residual items deferred to Part 4: exports.py exam enrichment cross-tenant, require_access(role,sub_category) helper, migration 014 not in run_all.py, db.otps drop, branch-scope convention enforcement. |
| 4 | **Multi-tenancy + Data Layer** | ✅ done | 9/9 stories complete, full BMad ceremony | 2026-05-15 | 2026-05-15 | Full ceremony: document-project (10-file docs suite), architecture (4 ADRs), epics+stories (4 epics, 9 stories, 100% FR coverage), readiness check (READY), dev (E1+E2+E3+E4), retro, adversarial review (10 findings, 4 fixed). Commits 332743f→d79f333. 387→420 tests passing (0 skipped). Key wins: require_access() canonical auth helper, 35 AI tool scoped_query migrations, SCHOOL_ID startup guard, audit gate fail-open, migration 014 fix, otps dropped, 10-file docs/ suite generated. |
| 5 | **Notifications + Real-time (SSE)** | ✅ done | 6/6 sprint stories complete + P5.0/P5.4/P5.7 coverage | 2026-05-15 | 2026-05-15 | Canonical notification service with fail-open logging, route + AI notification migration, bounded fan-out, notification pagination meta fix, mark-all-read race boundary, compound index migration 019, chat keepalive data events, service keepalive loop, SSE session normalization, frontend stream_error retry path. Backend 450 tests passing; frontend build passing with existing warnings. |
| 6 | **File Storage + Uploads** | ✅ done | 6/6 sprint stories complete + P6.7/P6.8 coverage | 2026-05-15 | 2026-05-15 | Authenticated and school-scoped file serving, owner/admin-or-self access model, S3 tenant key prefixes, file_uploads schoolId backfill migration 020, role-based upload limits, manual magic-byte MIME validation, orphaned S3 rollback records, chat upload blocklist with ephemeral docs, optional persistence/audit for generated PDFs, scoped paginated list/delete, S3 error logging. Focused Part 6 suite: 32 tests passing. |
| 7 | **Observability + Audit** | ✅ done | 6/6 sprint stories complete + audit helper migration | 2026-05-15 | 2026-05-15 | Canonical fail-open audit helper, zero direct audit inserts outside the helper, branch-scoped/paginated audit reads, audit indexes migration 021, structured logging extras, auth success/failure logs without secrets, consistent error response shapes, slow-query instrumentation, S3/SMS readiness checks, and audit coverage matrix. Focused Part 7 suite: 79 tests passing. |
| 8 | **Frontend Foundation** | ✅ done | 10/10 stories complete | 2026-05-15 | 2026-05-15 | Jest/RTL setup, chat stream manual retry semantics, non-chat SSE reconnect/backoff, ConfirmActionCard double-submit guard, DOMPurify style/class/event stripping, contained tool ErrorBoundary, ToolPage loading/error primitives, singleton 401 refresh redirect, URL `?tool=` routing, semantic tokens, attendance draft TTL purge. Frontend tests: 7 suites / 16 tests passing. Frontend build passing with existing warnings. |
| 9 | **Principal role (vertical)** | ✅ done | 8/8 stories complete | 2026-05-16 | 2026-05-16 | P9.1 PrincipalDailyOps lesson plan card; P9.2 leave approval notification+audit+idempotency; P9.3 class-summary + staff/today attendance endpoints (aggregation pipeline); P9.4 announcement broadcast gate (principal direct, audience injection guard); P9.5 lesson-plan-completion endpoint + exam access; P9.6 fee-collection-summary → principal access; P9.7 OWNER_ONLY_FIELDS staff PATCH guard (self-update privilege escalation closed); P9.8 incident POST any-user + PATCH resolve principal. 507→545 tests. Security bonus: 9 open routes found+fixed by surface test. |
| 10 | **Accountant role (vertical)** | ✅ done | 9/9 stories complete | 2026-05-16 | 2026-05-16 | P10.0 URL audit (0 extra 404s); P10.1 partial payments + receipt endpoint; P10.2 FeeSync idempotency + hung-job timeout + polling; P10.3 fee structure POST/PATCH (owner-only); P10.4 discount approval gate (Decimal threshold, pending_discount_approvals); P10.5 correction own-only guard + original_snapshot + correction_count; P10.6 expense auth → owner/accountant only + expense summary; P10.7 export 7→13 columns + N+1 batch fix; P10.8 payroll routes (new backend/routes/payroll.py + unique disbursement index). 545→598 tests (+53). |
| 11 | **Receptionist role (vertical)** | ✅ done | 5/5 stories + full ceremony | 2026-05-15 | 2026-05-16 | Party-mode review found 27 bugs — branch isolation (scoped_filter→scoped_query across all 13 handlers), force:true visitor bypass+rate-limit, on_behalf_of_phone DPDP masking, character/merit cert approval gate, is_overdue SLA flag, enquiry backward-transition EC-11.2 guard, receptionist ticket visibility fix, pending-checkout URL renamed. +13 tests. |
| 12 | **Maintenance Admin (vertical)** | ✅ done | 5/5 stories + full ceremony | 2026-05-15 | 2026-05-16 | Adversarial review found 17 bugs — SLA hours corrected (urgent:6h→4h, low:120h→168h), escalation rate-limit+status guard+owner notification, photos_append atomic $size guard (EC-12.3), cost-summary endpoint (MongoDB $sum null-safe), single-record GET /facility/{id}, upcoming schedule endpoint, overdue=true filter, calendar-correct monthly recurrence (not timedelta 30), is_overdue/due_at field renames. +13 tests. |
| 13 | **IT-Tech Admin (vertical)** | ✅ done | 5/5 stories + full ceremony | 2026-05-15 | 2026-05-16 | Adversarial review found 14 bugs — BLOCKER: IT-tech could reset owner password (fixed with target-role guard), BLOCKER: health/system made live Twilio/S3 calls (fixed), admin_reset_password audit log + no plaintext in response, admin_unlock_user clears lockout fields + audit, users_over_80_pct using per-user effective limit, GET /issues/tech uses require_access, branch_code unique DB index, POST /branches endpoint. +5 tests. |
| 14 | **Teacher role (vertical)** | ✅ done | 6/6 stories + full ceremony | 2026-05-15 | 2026-05-16 | Adversarial review found 10 bugs — CRITICAL: bulk results hard-aborted on first bad row (fixed: collect errors per-row, partial success shape), zero audit entries on bulk attendance (fixed: one entry/class), question papers missing schoolId (cross-tenant), /results/{id}/publish endpoint added, HoD scope bypass for cross-class assignments, /staff/me routing order fixed (FastAPI shadow), OWNER_ONLY_FIELDS for teacher class scope in student list. +8 tests. |
| 15 | **Student role (vertical)** | ✅ done | 5/5 stories + full ceremony | 2026-05-15 | 2026-05-16 | Adversarial review found 9 bugs — CRITICAL: SKIP_CONSENT_CHECK startup ValueError guard added, student IDOR on /fees/status/{id} fixed (ownership check), /fees/my summary block added (partial paid_amount counted), guardian annual_income/occupation stripped from student view, teacher class scope enforcement in student list. +3 tests. |
| 16 | **Platform Integration** | ✅ done | 6/6 stories + full ceremony | 2026-05-15 | 2026-05-16 | Adversarial review found 13 bugs — CRITICAL: health/ready never returned 503 on DB failure (fixed), 401 responses now include WWW-Authenticate: Bearer header, sms_logs created_at stored as native datetime (TTL was silently no-op on string), Motor maxPoolSize=50 configured, 4 performance indexes added (ptm_notes, question_papers, lesson_plans, curriculum_progress), 503 DB-down test. +1 test. |
| 17 | **AI Layer Hardening (Agentic Actions)** | ✅ Phase 1 COMPLETE (Epic G done; H gated on pilot sign-off) | A: 7/7, B: 4/4, C: 3/3, D: 6/6, E: 6/6, I: 3/3, F: 11/11, J: 2/2, K: 3/3, G: 8/8 | 2026-06-08 | — | Brownfield initiative: harden EXISTING AI tools so the main chat (`chat.py`/`/api/chat`, not the floating bubble) reliably PERFORMS actions with AI-action↔UI-action data parity (shared service layer + differential state-diff tests), agentic multi-step chaining under plan-then-confirm-once (atomic all-or-nothing + saga fallback), DPDP-compliant for children's data. Owner+Principal are the acceptance gate (Phase 1). No new tools/UI. PRD: `planning-artifacts/prd-ai-layer-hardening.md` (30 FRs, 25 NFRs, 7-job pilot inventory). Readiness check ✅ READY (0 critical). Architecture ✅ complete (`architecture-ai-layer-hardening.md`, 13 ADs incl. shared write-path, plan-then-confirm-once, atomic txn+saga, AD12 real-Mongo test tier; epics sequenced A→B→C→D). Epics & stories ✅ complete (`epics-ai-layer-hardening.md`, **11 epics / 53 stories**, FR1–FR42 mapped; +self-learning Epic G cloned from Odysseus; found 3 pre-existing AI defects → fixed in Epic B). **Shubham review #1 applied (2026-06-07):** dropped Story A.0; added Epics J/K (student/staff + school-internals CRUD) + Story F.10 (two-step destructive confirm + deletion audit); FR37–42 + AD15. **Review gate CLEARED (final feedback in from user + Shubham).** Plan final: 11 epics / 54 stories, FR1–FR43; Phase-1 = Owner+Principal only across the whole school DB; students unchanged. Readiness re-check (run 2, WITH epics) ✅ **GO** — 100% FR1–43 coverage, 0 critical, report `implementation-readiness-report-ai-layer-hardening-2026-06-08.md`. **EPIC A ✅ DONE (2026-06-08, one-epic-per-window):** Extracted 7 shared services (attendance, leave, approvals, announcement, contact-log, substitution, attendance-correction) + the pinned `actor_ctx` contract (`services/actor_context.py`, AD14). Both REST routes AND AI tools now call the SAME service (AD7 single-writer). Added 43 new tests incl. 7 dual-entrypoint parity tests under `tests/backend/parity/`. Suite **848 passed / 0 new failures** vs a pinned 25-failure pre-existing baseline (`ai-hardening-epic-a-baseline.txt`; documented as env/test-vs-code mismatches, NOT regressions; note: `stripe` dep had to be installed for collection). **Found-while-extracting fixes (beyond Epic-B's 3):** A.3 closed the AI approval routing-authz hole (principal/accountant could decide out-of-routing); A.4 brought the AI announcement gate into parity (owner/principal now broadcast directly, matching the route — see story doc, a notable behavior change); A.6 fixed AI substitutions (missing status, no dedup); A.7 fixed AI attendance-correction branch-scope bug. Story docs: `implementation-artifacts/ai-hardening/story-a{1-7}-*.md`. **EPIC B ✅ DONE (2026-06-08, one-epic-per-window):** Extracted 3 shared services — `services/fees_service.py` (record_payment), `services/discount_service.py` (apply_discount), `services/house_points_service.py` (award_points). Both the REST routes (`routes/fees.py` record_payment + apply_discount; `routes/activities.py` award_points) AND the AI tools (`tool_record_fee_payment`, `tool_apply_discount`, `tool_award_house_points` in `ai/tool_functions_v2.py`) now call the SAME service (AD7). **3 found defects resolved with permanent regression guards:** B.1 — AI fee payment had no idempotency (confirm retry double-charged) → service now keys off `student_id|fee_period|fee_head` + supports partial payments + emits SSE; B.2 — AI `apply_discount` bypassed owner approval on children's fees → threshold gate centralized in `discount_service`, above-threshold now parks in `pending_discount_approvals` on the AI path too; B.3 — AI `award_house_points` wrote an un-audited `house_points`-only model that never updated standings → now bumps `houses.points` + `house_points_log` + audit, matching the panel. Added 6 tests: `tests/backend/parity/{fees,discount,house_points}_parity_test.py` (dual-entrypoint parity + the 3 regression guards). Suite **855 passed / 0 new failures** vs the pinned 25-failure baseline. scoped_filter audit clean on both touched routes. **EPIC C ✅ DONE (2026-06-08, one-epic-per-window):** The 4 dynamic-collection tools (`assign_followup`, `update_incident_status`, `add_thread_entry`, `confirm_resolution`) historically picked their target collection at WRITE time by scanning `incidents`→`complaints`→`facility_requests`→`tech_requests` (`_find_mutable_record`). New `services/incident_service.py` exposes `resolve_record_type` (C.1 — explicit up-front resolution: legacy precedence preserved, unknown id → `IncidentNotFoundError`, same id in 2 collections → `IncidentAmbiguousError` hard refusal, no blind scan at write) + `assign_followup`/`add_thread_entry`/`update_incident_status`/`confirm_resolution` services. Both entrypoints now call them: AI adapters in `ai/tool_functions_v2.py` (resolve record_type, then call service) AND REST routes `routes/operations.py` (`/incidents/{id}/assign`, `/thread`, PATCH `/incidents/{id}`) + `routes/issues.py` (`/facility/{id}/confirm-resolution`). **Parity reconciliations (case-by-case, AI behavior canonical where richer):** REST thread route now writes the `add_thread_entry` audit (previously none); REST assign now appends optional note + audit + assignee notification (previously bare `$set`); confirm audit action canonicalized to `confirm_resolution` (legacy REST `facility_request_close` had no consumer); maintenance-can't-close guard centralized in the service. Removed the legacy `_find_mutable_record`/`_append_record_note` helpers. Added 12 tests: `tests/backend/parity/incident_resolution_test.py` (C.1 characterization, 8) + `incident_parity_test.py` (C.2/C.3 dual-entrypoint parity, 4). Suite **867 passed / 0 new failures** vs the pinned 25-failure baseline. scoped_filter audit clean (`incident_service` uses `scoped_query(branch_id=...)` throughout). **EPIC D ✅ DONE (2026-06-08, one-epic-per-window):** Safe execution — the transactional executor. **D.1** stood up the `@pytest.mark.mongo_real` replica-set tier (AD12): registered the marker + `-m "not mongo_real"` default deselect in `pytest.ini`, `tests/backend/mongo_real/` (conftest sourcing a replica set from `MONGO_TEST_URL` or testcontainers, else clean skip; README with the nightly/AI-path CI policy), and a `session=` no-op shim + opt-in unique-index enforcement on `FakeCollection`. **D.2** added `database.get_txn_session()` (real `start_session()`; `_NoopSession`/`_NoopTransaction` when no replica set so the executor has ONE code path), `ScopedCollection` already forwards `session=` while injecting `schoolId` (no tenant-leaking raw write inside a txn), and `services/txn_context.py` (contextvar + `session_kwargs`) so the 10 Epic A–C services auto-enlist in the executor's txn without signature changes. **D.3** added `ai/plan_schema.py` (`Plan`/`Step` + `single_write_plan`) and `ai/plan_executor.py` (`run()` transaction-first); `_execute_confirmed_dispatch` now builds a length-1 plan and calls `plan_executor.run()` UNCONDITIONALLY (no len==1 fork) — the 102 existing confirm/chat/attendance E2E tests pass through the new path. **D.4** per-step `idempotency_key=f"{plan_token}:{step_idx}"` claimed in-txn against the new UNIQUE index `ai_write_idempotency.idempotency_key` (`_create_indexes` + migration `025_ai_write_idempotency_index.py` + run_all) → exactly-once; AI fee key derivation matches `routes.fees._normalize_fee_key` (AD14 REST-key parity). **D.5** saga for non-Mongo side effects (run AFTER commit, compensate in reverse; compensation failure → `needs_manual_reconciliation` 409 + audit hook). **D.6** per-write `precondition` re-read inside the txn → `plan_stale` 409 (distinct from `plan_tampered`), no partial write. chat.py maps PlanStale/NeedsManualReconciliation/SagaCompensated to the P7 taxonomy. Added 24 tests (4 mongo_real files: txn sample, tenant-scope, executor rollback, idempotency concurrency, precondition; + FakeDb-tier shim/executor/idempotency/saga/precondition). Suite **891 passed / 8 mongo_real deselected / 0 new failures** vs the pinned 25-failure baseline. scoped_filter audit clean (chat.py hits are pre-existing conversation scoping; executor uses `scoped_query(branch_id=...)`). Story docs: `implementation-artifacts/ai-hardening/story-d{1-6}-*.md`. **EPIC-CLOSE REVIEW ✅ (2026-06-08, NEW mandatory step):** Per user directive the protocol now ends every epic with a thorough multi-lens BMAD review that fixes all in-epic bugs (EPIC-EXECUTION-PROTOCOL.md STEP 4). Ran it as a COLLECTIVE pass over Epics A–D (4 parallel lenses: adversarial-general, edge-case-hunter, testarch test-review+trace, testarch-nfr). **9 real findings fixed + 3 dismissed-with-reason** (`story-AD-review.md`). Top fix (CRITICAL): `create_notification` + `write_audit_doc` Mongo inserts now thread the ambient executor txn session (`session_kwargs()`) so a notification/audit for a rolled-back AI write rolls back too (AD14) — proven on mongo_real. Also: attendance bulk per-record failure now aborts the txn under a session (all-or-nothing) while preserving REST per-record reporting; executor `already_applied` broadened to WriteConflict/TransientTransactionError; idempotency claim skipped in dry-run + compensated on the noop path; saga escalates an uncompensatable completed side-effect to needs_manual_reconciliation; `routes/fees._normalize_fee_key` delegates to the single-source service key; numeric coercion in fees/house-points now raises domain 400s not opaque 500s; precondition gained nested-field + malformed-warning + version default. +22 regression tests (`test_ad_review_regressions.py` ×12, `mongo_real/test_review_atomicity_ad.py` ×2, + strengthened assertions). Suite **901 passed / 10 mongo_real deselected / 0 new failures** vs the pinned 25-failure baseline. **EPIC E ✅ DONE (2026-06-08, one-epic-per-window):** Whole-job-by-instruction — the agentic planner + plan-then-confirm-once. **E.1** evolved `services/confirm_tokens.py`: `compute_plan_hash()` (canonical sorted-key sha256 over the resolved plan + `school_id`/`branch_id`, same helper at issue & consume), `issue_confirm_token(plan=…)` persists `plan`/`plan_hash`/`schema_version` (`PLAN_SCHEMA_VERSION=1`), `consume_confirm_token` revalidates the hash → `plan_tampered` 409; a legacy no-plan token still consumes as a length-1 plan (back-compat by data). **E.2** new `ai/planner.py` `build_plan()` — model proposes, server authorizes/resolves/executes; deterministic via an injected `request_plan` (recorded fixtures, NO live Azure); produces ordered P3 step dicts (`idx/tool/kind/params/precondition`); read steps classified vs `WRITE_ACTION_TOOLS`; per-write `precondition` derived from the step's target (`_PRECONDITION_TARGETS`). **E.3** `MAX_PLAN_STEPS=8` bounds plan SIZE, distinct from `MAX_TOOL_ROUNDS=3` (read/plan rounds); confirmed-write execution (`/confirm`, a separate request) consumes neither; an over-long plan is rejected with a clear message. **E.4** server-side entity resolution via the scope-aware `_resolve_params` — `>1` match → disambiguation prompt + NO token; ANY unauthorized step → whole plan rejected with which-step feedback (AD14, never truncated); resolution-internal `_`-keys stripped before hashing. **E.5** `ai/plan_schema.plan_from_steps()` + a multi-step branch in `_execute_confirmed_dispatch`: one plan-confirm token, one `confirm_action` SSE event listing every step (`_build_plan_confirm_event`), atomic execution through the SAME `plan_executor.run()` path as length-1, whole plan re-authorized step-by-step at confirm, ONE rate-limit dispatch (checked once at `/confirm`), notifications/side-effects fire only after commit (executor saga), and a token that expired while the user read the plan → `plan_expired` 409 (re-planable, not a bare 400). **E.6** `_stream_plan` graceful fallback — deep-link `navigate` event to the matching UI panel (`_TOOL_DEEP_LINKS`) on cannot-plan/unauthorized/too-long, no partial write, logged. The compound-plan path fires only when the model proposes >1 tool call with ≥1 write (the single-tool flow is unchanged). Added 22 tests: `unit/test_confirm_token_plan_e1.py` (6), `unit/test_planner_e2.py` (7), `unit/test_plan_multistep_e5.py` (3), `unit/test_stream_plan_e5_e6.py` (2), `api/test_chat_plan_confirm_e5.py` (2), `mongo_real/test_plan_from_steps_atomic_e5.py` (2, replica-set atomicity). **EPIC-CLOSE REVIEW ✅:** multi-lens pass, 4 findings fixed + 3 dismissed-with-reason (`story-E-review.md`): expired-token re-planable 409, confirm-time whole-plan re-authorization, internal-key strip from plan/hash, empty-message + cannot-plan deep-link. Suite **923 passed / 10 mongo_real deselected / 0 new failures** vs the pinned 25-failure baseline. scoped_filter audit clean (chat.py hits pre-existing conversation scoping; planner reuses scope-aware `_resolve_params`). Story doc: `implementation-artifacts/ai-hardening/story-E-review.md`. **EPIC I ✅ DONE (2026-06-08, one-epic-per-window):** Frontend — multi-step plan card & status messaging (no new page/surface; evolves `ConfirmActionCard.js`/`ChatInterface.js`). **I.1** `ConfirmActionCard.js` `PlanSteps` renders all N steps of an `is_plan` `confirm_action` event in order (1-based index, per-step display, `Destructive` badge) under ONE Confirm + ONE Cancel; existing `submittingRef` double-submit guard reused; Cancel posts `decision:"cancel"` (no write) and reports cancellation. **I.2** `classifyConfirmError(status, body)` maps the backend 409/502/500 taxonomy (`plan_tampered`→re-ask, `plan_stale`/`plan_expired`→re-plan, `needs_manual_reconciliation`→manual attention, `side_effect_failed`→records-written-but-side-effect-failed, opaque→correlation-id-only) to distinct human messages; non-recoverable codes hide Retry; opaque failures leak NO internal string (id parsed from a top-level field, the detail object, or an opaque `(id=…)` 500 string). **I.3** new `ChatFollowup.js` + SSE wiring: `_resolve_params` attaches `_resolution_options` (student/staff/search, ≤5 `{label,value}`, value=admission-no else id; underscore-prefixed → stripped from plan_hash), `PlannerResult.options` carries them, `_stream_plan` emits a structured `{type:"disambiguation",message,options}` event (no token/write) and keeps the `{navigate,url}` deep-link; the chat renders selectable chips (picking re-sends the value to continue the flow) and a click-to-open deep-link card (`toolFromDeepLink` → existing `eduflow-navigate`, never an auto-jump). **EPIC-CLOSE REVIEW ✅:** multi-lens pass, 6 findings fixed + 4 dismissed-with-reason (`story-I-review.md`): nested-`detail` taxonomy parse (was `Request failed (409)`), opaque-500 string non-leak, `navigate.url` deep-link rendering (was dropped), empty-options dead-end guard, value-less-pick dead-end guard, dead style removed. +3 backend tests (`test_planner_e2.py` options ×2, `test_stream_plan_e5_e6.py` disambiguation event ×1) + 21 frontend tests (`ConfirmActionCard.test.js` I.1/I.2, `ChatFollowup.test.js` I.3). Backend suite **926 passed / 10 mongo_real deselected / 0 new failures** vs the pinned 25-failure baseline; touched frontend suites green (pre-existing unrelated `LayoutRouting.test.js` failure confirmed on the clean tree). scoped_filter audit clean (chat.py hits pre-existing conversation scoping; I.3 additions use the approved `_scoped`→`scoped_query` helper). Story docs: `implementation-artifacts/ai-hardening/story-i{1,2,3}-*.md` + `story-I-review.md`. **EPIC F ✅ DONE (2026-06-08, one-epic-per-window):** Compliant & operable — DPDP + safety-ops + parity harness. **F.1** `ai/redaction.py:redact_for_llm()` is now THE canonical redactor (`_safe_tool_result_for_chat` delegates) applied before LLM calls AND trace persistence; masks special-category keys only (DOB/contact/health/full-address/Aadhaar + secrets) while names/ids/counts/amounts pass through — **deliberately surgical so the LLM never over-blocks** (user directive). **F.2** `_audit_minor_read` + `MINOR_READ_TOOLS` write a PII-free `minor_record_read` audit row for every assistant read of a student record (FR21, fail-open); `contains_unredacted_pii()` scans traces for raw Aadhaar/phone. **F.3** `plan_executor._assert_step_scope` re-scopes each write step to the plan's `(school_id, branch_id)` → `PlanScopeViolationError`→403 if a later step names a foreign branch/school (no partial write). **F.4** `services/ai_kill_switch.py` (`db.system_flags.ai_writes_enabled`, fails OPEN, ≤30s cache) checked before token consume in `_execute_confirmed_dispatch` → 403 `ai_writes_disabled`; reads never gated. **F.5** `services/ai_shadow_mode.py` (`ai_dry_run`, default OFF) → executor runs writes in an always-aborted txn, reports `would_change`, no side-effects (true no-commit on mongo_real). **F.6** `tests/backend/parity/{normalizer.py,corpus.py,test_parity_corpus.py}` — shared normalizer ruleset (unit-tested) + a CI drift gate that fails if any `WRITE_TOOL_NAMES` tool lacks a parity corpus entry (14/14 mapped). **F.7** `services/ai_metrics.py:record_ai_metric`→`db.ai_metrics` emits PII-free counters (plan_executed/confirmation/step_outcome/ai_action/torn_state/kill_switch_blocked), fail-open. **F.10** two-step destructive confirm — a destructive step needs `destructive_ack=True` on the 2nd `/confirm` (else 409 `destructive_confirmation_required`, token NOT burned); commit writes an actor-tagged `action="delete"` audit; `FORBIDDEN_AI_TOOLS` (delete/erase student) refused outright. **F.11/FR43** Phase-1 lockdown — `services/ai_action_policy.py` single switch `LOCKDOWN_ENABLED`, applied in `_is_tool_authorized`: AI write/action tools = Owner + Principal only; all reads (incl. student) unaffected; Phase 2 (Epic H) widens by editing the one policy, no engine change. **F.8** closeout — this tracker + `project-context.md` (Epic F invariants table) + `docs/deployment-runbook.md` §8 (kill-switch / shadow / **F.9 revert-a-bad-AI-write runbook**) updated. Added 41 new tests (`unit/test_ai_redaction_f1_f2.py`, `test_executor_scope_f3.py`, `test_ai_flags_f4_f5.py`, `test_ai_action_policy_f11.py`, `test_ai_metrics_f7.py`; `parity/test_normalizer.py`, `test_parity_corpus.py`; `api/test_chat_dispatch_epicF.py`). Suite **967 passed / 10 mongo_real deselected / 0 new failures** vs the pinned 25-failure baseline. scoped_filter audit clean (chat.py hits pre-existing conversation scoping; new code uses scoped_query/get_school_id). **EPIC J ✅ DONE (2026-06-08, one-epic-per-window):** Owner/Principal CRUD — student & staff records as hardened AI tools wrapping the same shared services (AD7/AD15). **J.1** extracted `services/student_service.py` (`create_student`, `update_student`, `upsert_guardians`, `set_student_status` on the P1 signature + domain exceptions); refactored `routes/students.py` create/update/guardians into thin adapters; added 4 AI tools (`create_student`, `update_student`, `manage_student_guardians`, `set_student_status`) — **no `delete_student`/`erase_student` AI tool** (hard-delete + DPDP-erase stay UI-only; assistant relies on tool absence + the existing deep-link fallback). **J.2** extracted `services/staff_service.py` (`create_staff` incl. auth-user create/link + temp-password + privileged-account gate; `update_staff` with OWNER_ONLY_FIELDS silent-strip, leave-balance/accounts authority, auth_users user_info sync); refactored `routes/staff.py` create/update into thin adapters; added `create_staff`/`update_staff` AI tools. **All 6 J tools registered Owner+Principal-only** (`roles=["owner","admin"], sub_categories=["principal"]`) and pass the F.11 Phase-1 lockdown; added to `WRITE_TOOL_REQUIRED_PARAMS` + labels (confirm-gate coverage). **Security:** AI `create_staff` never surfaces the plaintext temp password to the LLM/chat (REST still returns it once for the operator); DB state identical → parity holds. Both stories proven byte-identical via dual-entrypoint parity (`tests/backend/parity/{student,staff}_parity_test.py`, 6 new corpus entries → CI drift gate green). **EPIC-CLOSE REVIEW ✅ (`story-J-review.md`):** multi-lens pass, 8 findings fixed (2 HIGH = removed orphaned duplicate write-path helpers in both routes that would have drifted from the services — the core AD7 risk; 1 HIGH = confirm-gate required-params gap; 1 HIGH = temp-password leak guard) + 4 dismissed-with-reason. +38 net new passing tests incl. `tests/backend/unit/test_epic_j_crud_guardrails.py` (privilege-escalation block, OWNER_ONLY strip on AI path, no-op/dup/non-list edges, lockdown allow/deny matrix). Suite **1005 passed / 12 deselected / 0 new failures** vs the pinned 25-failure baseline. scoped_filter audit clean (student/staff are a whole-school domain; refactored handlers contain no inline scoping — delegated to services; remaining hits are pre-existing school-scoped reads/untouched routes). Story docs: `implementation-artifacts/ai-hardening/story-j{1,2}-*.md` + `story-J-review.md`. **EPIC K ✅ DONE (2026-06-08, one-epic-per-window):** Owner/Principal CRUD — school internals as hardened AI tools (AD7/AD15, no new UI). **K.1** extracted `services/fee_config_service.py` (fee structures + discount types); refactored `routes/fees.py` create/update structure + create/update discount-type into thin adapters + added a `DELETE /discount-types/{id}` parity route; 5 AI tools (`create_fee_structure`, `update_fee_structure`, `create_discount_type`, `update_discount_type`, `delete_discount_type`); fee-structure create/update now audited (AC) on BOTH entrypoints. **K.2** built `services/academic_structure_service.py` (classes incl. section field + houses) — these had NO write REST routes (classes came only from seed/import; houses GET-only), so per the approved scope decision **new service-backed REST routes were added as the parity reference** (`POST/PATCH/DELETE /api/settings/classes`, `POST/PATCH/DELETE /api/activities/houses`) — no new UI; 6 AI tools. **K.3** extracted `services/org_config_service.py` (branches + school settings + year-end transition); refactored `routes/settings.py` create/upsert branch + school PATCH + year-end into thin adapters + added a `DELETE /branches/{id}` parity route; 5 AI tools, **owner-only** (org config stays owner-only even in Phase 2, AD15). **Lockdown:** K.1/K.2 tools = Owner+Principal (`roles=["owner","admin"], sub_categories=["principal"]`); K.3 = `roles=["owner"]`. **Destructive (F.10):** `delete_discount_type`, `delete_class`, `delete_house`, `delete_branch`, `year_end_transition` flagged `destructive:True` → two-step confirm + actor-tagged deletion audit; class/house/branch deletes blocked when active students are assigned. All 16 tools added to `WRITE_TOOL_REQUIRED_PARAMS`+labels (confirm-gate coverage) and to the parity corpus (CI drift gate green). 3 dual-entrypoint parity modules (`tests/backend/parity/{fee_config,academic_structure,org_config}_parity_test.py`) prove byte-identical AI↔REST. **EPIC-CLOSE REVIEW ✅ (`story-K-review.md`):** multi-lens pass, 6 findings fixed (2 HIGH = `create_class` branch-injection privilege escape for a branch-scoped principal → owner-only cross-branch targeting [NFR5]; shared-singleton test-isolation leak where parity fixtures wiped seeded `classes`/`academic_years` and broke Epic J student-create FK → save/restore fixtures) + 6 dismissed-with-reason. +~120 net new passing tests incl. `tests/backend/unit/test_epic_k_crud_guardrails.py`. Suite **1121 passed / 12 deselected / 0 new failures** vs the pinned 25-failure baseline. scoped_filter audit clean (only pre-existing school-scope helpers on touched routes; services use scoped_query(branch_id) for classes, scoped_filter for school-scoped domains). Story doc: `implementation-artifacts/ai-hardening/story-K-review.md`. **EPIC G ✅ DONE (2026-06-08, one-epic-per-window) — AI self-learning (Memory + Skills), cloned from Odysseus, Owner/Principal only:** **G.1** infra spike (`story-G.1-spike.md`) → **GO, keyword-first; vector (`chromadb`+`fastembed`, `BAAI/bge-small-en-v1.5`) behind `MEMORY_VECTOR_ENABLED`, default OFF** (onnxruntime ~200 MB + cold-start model download risky on small EB) — fallback clause honored. New `services/memory/` package (cloned from Odysseus `src/memory.py`, `memory_vector.py`, `skills.py`, `skill_extractor.py`, `memory_extractor.py`): `retrieval.py` (pure-Python keyword scoring + confidence/recency decay), `vector.py` (graceful-degradation wrapper, `.healthy=False`→keyword-only), `store.py`, `skills_store.py`, `extractor.py`, `chat_integration.py`. **G.2** `ai_memories` Mongo collection scoped `(user_id, schoolId)`; `ai.redaction.redact_text_for_memory()` (new: scrubs raw Aadhaar/phone/email from free text) on write; dedup; `write_audit`. **G.3** `store.recall()` hybrid (vector ∪ keyword, re-ranked by deterministic keyword score, degrades to keyword-only — FR33) injected into the system prompt at chat Phase 4 (`recall_context_block`). **G.4** auto-save via `extractor.extract_memory_items` (confidence ≥0.75 saved silently, [0.4,0.75) → in-chat yes/no appended to the reply, no UI — FR32) + inline `remember:`/`forget` + affirmative-confirm of a `pending_memory` on the conversation doc; runs at chat Phase 1b (pre-LLM commands) + Phase 9b (post-LLM finalize). **G.5** `tool_recall_history` (registry, Owner/Principal-gated, in `MINOR_READ_TOOLS`) — synthesis that DELEGATES to existing read tools (`tool_get_student_profile`/`get_fee_transactions`/`get_enquiries`) for identical authz/scope, so it can surface nothing the caller couldn't already read; minor-read audit fires (FR35). **G.6** `extractor.extract_skill` (≥2 rounds OR ≥2 tools, else zero LLM calls) + `ai_skills` store (confidence floor 0.6, dedup-by-title, in-chat helpful/not feedback, `(user_id,schoolId)` isolation, skill text PII-scrubbed). **G.7** `erase_owner_memories`/`erase_owner_skills`, `purge_student_references` (wired into `routes/students.py:erase_student` — DPDP §12), `prune_expired` + Date `expire_at` TTL index (540-day retention; migration `026_ai_memory_skills.py` + `_create_indexes`). **G.8** `correct_memory` (update/remove + confidence boost on correction) + confidence/recency decay in retrieval ranking. **EPIC-CLOSE REVIEW ✅ (`story-G-review.md`):** multi-lens pass, **5 findings fixed** (2 HIGH = over-aggressive intent detection: bare mid-sentence "actually" silently deleted a memory; "ok <request>" treated as a bare confirmation and swallowed the real request → both regexes made conservative/anchored; 1 MED = skill text now PII-scrubbed before LLM replay; 1 MED = TTL was on an ISO-string field that never expires → real Date `expire_at`; 1 LOW = array-contains purge made tier-agnostic) + accepted notes (per-turn extraction LLM cost for the pilot; vector blocking-embed default-OFF). +31 new tests (`tests/backend/unit/test_ai_memory_skills.py`, all 8 stories' ACs + 4 regression). Suite **1148 passed / 12 deselected / 0 new failures** vs the pinned 25-failure baseline. FR32 (no UI) asserted (no `/memory`,`/skills` route). scoped_filter audit clean (memory queries carry explicit `schoolId`+`user_id` in the service layer; student-erase purge is intentionally school-wide). **▶ PHASE 1 COMPLETE.** Epic H (Phase 2, role extension to all staff) is **GATED on the Owner+Principal pilot sign-off** — do NOT auto-start it. **POST-PHASE-1 DRIFT REMEDIATION (2026-06-10):** commit ff2e929 had added 4 AI write tools (create_expense/create_enquiry/update_enquiry_status/create_incident) bypassing the F.6 drift gate (no corpus entry, no confirm-gate params, direct-write bodies that diverged from REST). Fixed: extracted `services/{expense,enquiry}_service.py` + `incident_service.create_incident` (AD7 single-writer), rewrote the 4 tools as thin adapters, added them to WRITE_TOOL_REQUIRED_PARAMS + PARITY_CORPUS. Also closed owner coverage gaps with 7 new hardened tools: update_expense, delete_expense (destructive), mark_staff_attendance (new `staff_attendance_service.py`, supports mark-all-active-staff), correct_fee_transaction, delete_fee_transaction (destructive, soft-delete), trigger_fee_sync (new `fee_sync_service.py`), get_fee_sync_status (read). REST routes refactored onto the same services. 91 tools total, 46 write tools, all auto-covered by F.11 lockdown + F.4 kill-switch (derived from registry flags). 3 new parity modules (`ops_crud`, `staff_attendance`, `fee_txn`) — 16 tests incl. enquiry transition-guard regression (legacy AI tool skipped it). Stale tests updated for ee92f4e (SMTP removal, 500-cap pagination) + fee soft-delete. Suite 1161 passed / 0 new failures vs pinned baseline. **WAVE 2 (2026-06-10):** owner coverage extended to the remaining owner-modifiable surfaces — 5 new shared services (`asset_service`, `visitor_service`, `certificate_service`, `query_ticket_service`, `transport_service`) + `announcement_service.decide_announcement/delete_announcement`; 19 new hardened AI tools (assets CRUD, visitor log/checkout/delete, certificate create/decide, query ticket create/resolve/reopen/assign/delete, transport route CRUD + vehicle, announcement approve-reject/delete). REST routes in operations.py + queries.py refactored onto the services (AD7). Transport route delete now blocked while active students assigned (K-review safety rule, both entrypoints). Registry: **110 tools / 65 write / 12 destructive**, all auto-covered by F.11 lockdown + F.4 kill-switch. 2 new parity modules (`ops_admin`, `query_transport`, 18 tests). Suite **1179 passed / 0 new failures** vs pinned baseline. Still UI-only by design: data import (file flow), payroll disbursement, SMS/WhatsApp sends (outward comms), academics teacher surfaces (assignments/lesson-plans/worksheets), maintenance vendors/schedule, council/teams — queued as Wave 3 if wanted. |
| 19 | **Platform Reliability (Non-AI, R12–R15)** | ✅ ALL DONE — R12–R15 COMPLETE (2026-07-10) | R12: 5/5 · R13: 9/9 · R14: 2/2 · R15: 5/5 | 2026-07-10 | 2026-07-10 | Companion initiative to #18 — the non-AI platform surfaces (onboarding, billing, tenancy/RBAC, multi-worker, and a residual line-audit). **R12** onboarding/billing/payroll integrity (provisioned-owner login, Razorpay cross-tenant credit guard, atomic top-up credit + `personal_topups` path fix, single payroll implementation). **R13** tenancy & RBAC fail-closed (ScopedCollection method gap, file-serve/export least-exposure, per-school login lockout, staff-deactivation session revocation, SMS daily cap, `re.escape` on all user-supplied regex operands). **R14** multi-worker correctness (SSE startup guard — refuses `WEB_CONCURRENCY>1` without Redis; school-status TTL cache; layaastat task-set GC guard). **EPIC R15 — Residual Confirmatory Sweep ✅ DONE (2026-07-10) — FINAL EPIC:** a confirmatory line-level pass over the remaining lower-risk route/service files. **R15.1** the in-app AI token spend can no longer drive a personal top-up balance negative — the debit is now an atomic `max(0, balance − used)` clamp (a big turn or a burst of concurrent turns floors at zero, never overdraws). **R15.4** timestamps written by the shared service layer are now timezone-aware UTC (they were naive, which made date comparisons ambiguous across servers); the idempotency cache won't store an oversized response body (protects against a document growing past Mongo's 16 MB limit); and the diagnostic `seed-status` endpoint (which exposed row counts to anyone) is now owner-only in production. **R15.5** a duplicate manual attendance entry now returns a clear "already recorded" message instead of a server error (a same-status resubmit is accepted quietly); the four default houses are seeded idempotently so two simultaneous first-loads can't create duplicates (backed by a new unique index). **R15.3** the floating in-app help assistant — previously uncapped and unmetered — now has a per-user hourly limit and records its token usage, closing a runaway-cost surface; its "AI unavailable" path was confirmed to return a real error (not a fake success). The deep audit's remaining items were triaged: ~20 "missing schoolId" flags were false positives (ScopedCollection auto-injects schoolId — only branch scoping is explicit); leave/announcement/user school-wide queries confirmed intentional (tracker line 64); guardian-contact visibility to teachers routed to the owner as a product decision; and three low items (a readiness-check blind-spot, an unbounded admin token-usage read, and the wider naive-datetime migration) logged as tracked follow-ups. Suite **1616→1632 passed / 0 failed / 14 deselected** (+16 tests; the 14 deselected are the credentialed `mongo_real`+`llm_eval` tiers, not failures — the 25 pinned baseline failures remain non-reproducing). Quality gate clean: adversarial + edge-case + trace + NFR review, branch-scope grep audit, no prompt/tool eval-gate trigger. Logs: `implementation-artifacts/ai-reliability/epic-R15-completed.md` + `epic-R15-review.md`. |
| 18 | **AI Layer Reliability (Zero Silent Failures)** | ✅ ALL DONE — R1–R11 COMPLETE (2026-07-10) | R1: 7/7 · R2: 6/6 · R3: 4/4 · R11.1: 1/1 · R4: 4/4 · R5: 3/3 · R6: 4/4 · R7: 3/3 · R8: 4/4 · R9: 5/5 · R10: 5/5 · R11: 5/5 | 2026-07-10 | — | **EPIC R11 — Excellence & Evaluation ✅ DONE (2026-07-10) — FINAL EPIC:** Native Azure function calling eliminates invented-tool-name hallucinations at the API level (model can only call a tool that exists in the authorized schema list — R11.2). True token streaming delivers first-word responses in real time instead of all-at-once (R11.3). A 5-line language directive enables natural Hinglish/Hindi responses; eval corpus expanded to 52 conversations with ≥10 Hinglish + ≥2 Devanagari entries (R11.4). A per-turn conversation trace viewer (owner-only, school-scoped) makes the "AI didn't reply" incident class diagnosable from the panel without server-log access; provider/model identity (Azure, GPT-4.1) is hardcoded as "Layaa AI" on every client-facing surface — Layaa AI confidentiality enforced by construction (R11.5). Residual audit hardening: idempotency replay asserts schoolId as defence-in-depth; ai_metrics PII denylist widened from 7 to ~20 synonyms (student, guardian_phone, etc. — R11.6). 4 larger findings deferred with documented reasons (rate-limiter ScopedCollection gap, actor_context naive clock, sse eviction, shadow_mode cache). Suite **1527→1551 passed / 0 failed / 0 skipped** (+24 new tests); evals 18 passed; frontend ConversationTrace tests pass. Quality gate clean: adversarial review, AC trace, scoped_filter audit, provider-leak grep all pass. **INITIATIVE COMPLETE — all 11 epics shipped. Suite 1278→1551 passed / 0 new failures total.** Logs: `implementation-artifacts/ai-reliability/epic-R11-completed.md` + `epic-R11-review.md`. — **EPIC R10 — Self-Learning Phase 2 ✅ DONE (2026-07-10, Abhimanyu's full-R10 go-ahead / owner gate-override):** the assistant now genuinely gets better with use, with the owner in full control of everything it learns. New **"What I've Learned"** screen (owner/principal only) lists every remembered note, saved routine, and pending suggestion — edit, deactivate/reactivate, or delete any of it; bulk-delete confirms first. **R10.2** 👍/👎 feedback now feeds a real learning loop: "Improve" + a note creates a *pending suggestion* that does nothing until you Activate (or Reject) it, and each person only sees the notes they themselves flagged (no cross-user leak). **R10.3** the assistant no longer silently saves "routines" — it *asks* first and only saves on your yes; a routine relying on a changed tool is flagged "needs updating" rather than replaying a stale plan; write-embedding routines still confirm every change. **R10.1** recall made scalable (indexed, paginated — no full scans). **R10.4** a reply that used a remembered note now shows a "🧠 remembered" line in its "Data used" footer. **R10.5** a single greppable switch controls which roles get memory — stays owner/principal only (widening is a future config decision; widened roles get read-only recall first). Epic-close review found + fixed 6 issues each with a regression test, incl. a cross-user privacy leak (pending list briefly school-wide → now per-reviewer) and a silent data-loss on correction activation. Suite **1464→1526 passed / 0 failed / 0 skipped** (+62 tests); eval tier 18 passed; frontend R10 tests pass (only the 2 pre-existing `LayoutRouting` tests remain red, deferred). **Keep learning paused via the kill-switch until the panel + turn-outcome/feedback signals are watched in real use for ~2 weeks (owner's standing instruction).** Only R11 remains (gated). Logs: `implementation-artifacts/ai-reliability/epic-R10-completed.md` + `epic-R10-review.md`. — **EPIC R9 — Guardrails, Config & Adjacent Surfaces ✅ DONE (2026-07-10):** the AI's config fails loud instead of degrading silently, its safety filters stopped over-blocking legitimate work, its emergency off-switch takes effect instantly everywhere, and its two side features (file uploads, certificate/ID-card generation) are locked down. **R9.1** the exact env-var name mismatch from the incident audit is fixed — the Azure key is read under either documented name, and outside development the server now REFUSES to start if the AI key/endpoint is missing (like it already does for the school id) instead of quietly running with a dead assistant; the health check now checks the key too. **R9.2** the student safety filter no longer runs over raw tool data, where a single flagged word (e.g. a book title) used to blank out an entire legitimate result; the "bomb" filter now allows history/curriculum ("the atomic bombing of Hiroshima") while still blocking how-to/threats; a student genuinely named "Dan" is no longer mistaken for a jailbreak; and blood group (printed on ID cards anyway) is no longer permanently hidden as "[restricted]" while real medical data stays hidden. **R9.3** flipping the AI-writes emergency switch OFF now stops writes on every server immediately (the write path re-checks the flag live) rather than up to 30s later on some servers; dropped analytics events are now logged loudly and can't be silently garbage-collected; and a new turn-outcome counter makes the silent-no-reply incident class alarmable. **R9.4** uploads are now size-checked before being read into memory (no more buffering a giant file first), zip files are checked for "zip bomb" expansion before extracting, and images pasted into chat are validated for type/size. **R9.5** only the owner or principal can now generate transfer/character certificates and ID cards (was any admin — a forgery risk), and their contents are pulled from the database by student id rather than trusted from the request (you can't mint a certificate for an arbitrary name); the Google image-generation dependency was removed entirely (it sent school data to Google, against our Azure-only data policy, and failed silently) in favour of the built-in design; plus a daily generation cap. Suite **1444→1456 passed / 0 failed / 0 skipped** (+12 tests); eval tier 18 passed. **R9 is the last epic before the gated ones — R10 (self-learning phase 2) and R11 (excellence) need Abhimanyu's explicit go-ahead.** Logs: `implementation-artifacts/ai-reliability/epic-R9-completed.md` + `epic-R9-review.md`. — **POST-MERGE REVIEW FIXES (2026-07-10):** before starting R8, re-audited the just-merged R5/R6/R7 code and found + fixed 1 medium tenancy leak + 8 low issues, each with a regression test. The medium one: a coordinator configured with a *reversed* class range (e.g. "5-1") would have been silently granted access to EVERY class in the branch — now denied. The rest: aliased/mis-cased admin roles now get their proper chat context; a KG in-charge name filter no longer over-matches; "note down attendance for class 5" is no longer swallowed as a memory note; a degraded-recall warning now actually reaches operators; fee "days overdue" can't go negative on future-dated dues; calendar dates are normalised so events aren't dropped; and exam pass-rates only display when every graded student can be scored. Backend suite **1433→1444 passed / 0 failed / 0 skipped** (+11 regression tests). — **EPIC R8 — Frontend Chat Resilience ✅ DONE (2026-07-10):** every way the chat screen could fail silently now shows the user something and a way to recover (audit FH1-FH5, FM1/FM3/FM4, FL). If your login quietly expires mid-message, the app refreshes and retries once, then shows a clear "session expired" message instead of doing nothing. If starting a new conversation fails, your typed message is put back in the box (not lost) with an error. If past messages fail to load, you get "couldn't load history — Retry" instead of a blank screen that looks empty. Switching conversations fully clears the previous one's confirm cards/prompts, so a confirmation can't fire against the wrong chat. When your AI usage runs out mid-question, you get a visible "you've reached your limit — recharge below" bubble instead of the question vanishing, and a failed recharge shows a retryable error (the input is never left permanently locked). A dropped connection auto-reconnects once before offering manual Retry, and Retry no longer posts your question twice. The assistant's formatted replies (headings, tables, links) render properly again — a security setting had been stripping all their styling — and links are now clickable and protocol-safe. Two deeper safety nets: the reply can no longer be silently dropped when a fast response arrives all at once, and confirm-card actions now survive an expired token. Frontend tests **43→47 passing** (+11 R8 tests); the only red tests are 2 pre-existing `LayoutRouting` ones that also fail on main (logged, deferred). Backend untouched, still **1444 passed**. Logs: `implementation-artifacts/ai-reliability/epic-R8-completed.md` + `epic-R8-review.md`. — **EPIC R7 — Data Correctness & Performance ✅ DONE (2026-07-08):** the numbers the assistant reports are now right, and its read tools are fast. **R7.1** the branch-comparison used the wrong (empty) attendance collection so cross-branch attendance always showed blank — fixed to read real attendance via each branch's classes; and the four places that computed "outstanding fees" with subtly different formulas (branch summary, defaulters list, smart-alerts, background context) now share ONE canonical helper, so every screen agrees — including a consistent definition of "defaulter" (any student with an unpaid/overdue/partial balance, not only strictly-overdue). **R7.2** removed the query storms where the assistant fired one database call per class/staff/student/leave/house in a loop (now batched) — most importantly the "students absent 3+ days" figure was silently capped at 50 students and therefore WRONG; it now covers everyone. **R7.3** assistant-created announcements now actually show up in the upcoming-events list; exam pass-rates use the real maximum marks instead of always assuming 100 (a 50-mark test no longer reports everyone as failing); a class attendance rate can never read above 100%; "open the fee panel" only triggers navigation when it's an actual command, not when the words appear mid-sentence; result counts are read from the real data; and a slow query no longer risks the reply stalling silently. Also standardised audit timestamps to UTC (was a naive/UTC mix). Suite **1433 passed / 0 failed / 0 skipped**; +8 tests. Logs: `implementation-artifacts/ai-reliability/epic-R7-completed.md` + `epic-R7-review.md`. — **EPIC R6 — Memory Subsystem Safety ✅ DONE (2026-07-08):** the assistant's memory feature can no longer hijack a turn, take orders from a saved note, leak private numbers, or delete notes without asking (audit X3/XM3/XM4/XM5/XM10). **R6.1** the big one: a command like "delete student Rahul Sharma" or "note attendance for class 5" was being swallowed by the memory feature before the real request ran ("Got it — I'll remember that") — now only explicit phrasings ("remember: …", "note to self: …") save a memory, and everything else goes to the normal action pipeline; a stale "yes" can no longer confirm an old memory prompt. **R6.2** deleting a saved note is now two-step — the assistant shows you exactly which note(s) it would remove and waits for your "yes", instead of substring-matching and deleting everything at once. **R6.3** saved notes are now injected as clearly-fenced *background reference* that explicitly cannot override the assistant's permissions or safety rules (closing a self-prompt-injection channel), and any follow-up "should I remember that?" text is scrubbed of raw phone/ID numbers before it's shown or stored. **R6.4** when a staff member's account is retired, the assistant now erases what it had learned about them (DPDP right-to-erasure); erasure/purge no longer silently stops at 5,000 records; each person's memory is capped (least-useful evicted, logged) so recall stays fast; and the memory index is rebuilt from the database on restart so it isn't silently empty after a deploy. Suite **1411→1425 passed / 0 failed / 0 skipped**; +14 tests. Logs: `implementation-artifacts/ai-reliability/epic-R6-completed.md` + `epic-R6-review.md`. — **EPIC R5 — Tenancy & Scope Fail-Closed ✅ DONE (2026-07-08):** empty or ambiguous permission scope can no longer widen access — it now fails closed (audit H4/X6/L6). **R5.1** every "list" tool the assistant uses (students, classes, fees, houses, transport, inventory, library, council…) now filters by the branch on the user's own login, not just a resolved scope that could be blank — so a branch-bound admin sees only their branch, while an owner/principal with no branch still sees the whole school. **R5.2** looking up a single student, awarding house points, or marking attendance can no longer reach across into another branch's records — including a defense-in-depth check that a directly-named class actually belongs to your branch before attendance is written. **R5.3** the permission resolver was hardened: a class-range coordinator for "Classes 1–5" no longer accidentally captures Classes 10/11/12; a head-of-department whose subject maps to zero classes now sees NOTHING (previously it silently opened up to every student in the school); fee/exam queries for class-scoped teachers fail closed; a subject name with symbols like "C++" no longer crashes the resolver; and class-teacher matching now accepts either the staff id or the login id. Also closed a related over-exposure where non-operational admins (IT, maintenance, etc.) were being shown the principal's school-wide chat context — they now get a minimal context. Suite **1396→1411 passed / 0 failed / 0 skipped**; +15 tests. Logs: `implementation-artifacts/ai-reliability/epic-R5-completed.md` + `epic-R5-review.md`. — **EPIC R4 — One Tool Envelope + Denied ≠ Empty ✅ DONE (2026-07-08):** every assistant tool now returns ONE uniform result shape so the assistant reads outcomes correctly (audit C1/C3/M1/M2/H5/L1/L3). **R4.1** a missing `import json` had been crashing the flagship "briefing" tool whenever there were admission enquiries, and the fees/enquiries sections were being silently dropped from briefings — both fixed. **R4.2** all 14 legacy tools + the newer tools now share the single `{success, data, meta, message, denied}` envelope; the old three-way mix of result shapes is gone, and a new CI check invokes every read tool to prove the shape (and that no tool crashes on empty/missing data). **R4.3** a permission refusal now clearly says "you don't have access" instead of the assistant misreporting "there are none", and a failed action (e.g., student not found on a points award) is reported as a failure, not a silent success. **R4.4** legacy tools are hardened against one bad record taking down a whole tool, guardian phone numbers are masked at source (last-3-digits rule) across defaulters/enquiries/transport, and the leave list now carries the id the approve action needs. Suite **1344→1396 passed / 0 failed / 0 skipped**. Logs: `implementation-artifacts/ai-reliability/epic-R4-completed.md` + `epic-R4-review.md`. — **EPIC R11.1 — Golden eval corpus + LLM-judge CI ✅ DONE (2026-07-08, pulled forward after R3):** built the quality-regression gate so every later epic ships measured. A golden corpus of **48 real conversations** (every role, the production incident, follow-ups, ambiguous asks, denials, Hinglish) now lives in `_bmad-output/test-artifacts/eval-corpus/`. Three tiers: (1) a deterministic **structural eval** (CI, no creds) that proves the corpus stays coherent with the live prompt↔registry wiring; (2) **judge-logic unit tests** (CI, no creds) locking the scoring/threshold maths with a fake judge — including that a silent/no-reply turn scores zero and is never excused; (3) a credentialed **LLM-judge gate** (`-m llm_eval`, deselected by default) that scores correctness/completeness/tone per rubric and **blocks release** if quality drops >0.05 below the recorded baseline. The execution protocol now REQUIRES a green eval run for any prompt/tool change (AC3). Suite **1326→1344 passed / 0 failed / 0 skipped** (+18 always-on eval tests; the credentialed tier is deselected, not skipped). The credentialed baseline is written on the first run in an environment with Azure keys. Logs: `implementation-artifacts/ai-reliability/epic-R11.1-completed.md` + `epic-R11.1-review.md`. — **EPIC R3 — Prompt ↔ Registry Parity ✅ DONE (2026-07-08):** the assistant can no longer be advertised a tool that doesn't exist, that the user's role can't use, or with the wrong parameters (audit C4/H1/H2/H3/L4/L5/XM8). **R3.1** accountants were being handed the *principal's* tools and context (a real data over-exposure) because the tool table keyed them as `accounts` while the canonical role name is `accountant` — fixed in both the tool table and the context builder. **R3.2** `award_house_points` now advertises the parameter the code actually needs (`student_name`, not the old `house_name`), the never-saved `category` field was dropped, three tool definitions that had been declared twice with conflicting schemas were de-duplicated to a single correct copy, and `search_students`/`get_student_profile`/`get_fee_transactions` no longer advertise filters the code ignores. **R3.3** the `get_announcements` tool students were being offered but which **did not exist** is now implemented (published-only, student-safe) and registered; `recall_history` is now offered to principals (the code already allowed it). **R3.4** a new permanent CI gate (`parity/prompt_registry_parity_test.py`) checks, for every role, that every advertised tool exists, is authorized, and has matching required params — merge-blocking, so this class of drift cannot silently return. Suite **1313→1326 passed / 0 failed / 0 skipped**. Logs: `implementation-artifacts/ai-reliability/epic-R3-completed.md` + `epic-R3-review.md`. — **EPIC R2 — Confirmed-Write Integrity ✅ DONE (2026-07-08):** a confirmed action's reported outcome now always equals its true outcome (audit X2/X4/X5/XM1/XM2/XM6/XM9). **R2.1/R2.2** the plan executor now treats a step that RETURNS `{success:False}` (or raises a domain unique-index `DuplicateKeyError`) as a failure — it aborts the transaction and surfaces a typed 422 naming the step ("no changes were applied"), and the write-ahead audit row records the same failure so reply and audit agree; `already_applied` is reserved for the idempotency-claim collision ONLY. **R2.3** outside `ENVIRONMENT=development` a failed `start_session()` now FAILS LOUD (503 `txn_unavailable`) instead of silently running a non-transactional no-op write; dry-run rollback proven on the mongo_real tier. **R2.4** the confirm-token plan hash is now HMAC-SHA256 keyed with `JWT_SECRET` (was forgeable unkeyed sha256), expiry/validation failures carry typed reason codes (no `"expired"` string-matching), and post-consume failures echo a PII-free intent for one-tap re-issue. **R2.5** post-commit metric/audit/message writes are wrapped so a committed plan never 500s (XM9); read steps are dropped from confirmed plans (XM2); saga machinery retained + documented (live notifications are txn-enlisted Mongo writes, XM1). **R2.6** new CI guard (`parity/write_classification_guard_test.py`) fails if a mutating tool is registered without write flags (closes the confirm/kill-switch/audit bypass, X7). Suite **1290→1313 passed / 0 failed / 0 skipped**; +23 tests. Logs: `implementation-artifacts/ai-reliability/epic-R2-completed.md` + `epic-R2-review.md`. — Post-incident initiative (an owner's chat turn returned NOTHING — no reply, no error). 11 epics / 51 stories; build R1→R2→R3→R11.1→R4…R9→(gated)R10→R11 per `EPIC-EXECUTION-PROTOCOL-AI-RELIABILITY.md`. **EPIC R1 — Turn Completion Contract ✅ DONE (2026-07-08, one-epic-per-run):** the incident fix. **R1.7** `llm_client.chat()` now returns a single `LLMResult(text,tokens,ok,reason)` dataclass — killed the tuple\|dict dual return that had persisted a raw tuple/dict as question-paper content (audit X1); migrated all 5 callers (chat.py ×3, academics, assistant, memory/extractor); academics `ok=False`→503, assistant `ok=False`→503. **R1.6** empty-content+`finish_reason=length` retries once with raised headroom (default `max_completion_tokens` 1200→4000, retry→8000); empty-after-retry returns a typed `ok=False` failure, never a "successful" blank. **R1.3** Phase-14 rewritten into a single completion choke point: the old `f"empty-{conv_id}"` no-persist/no-debit short-circuit is DELETED — an empty turn now substitutes `FALLBACK_TEXT`, streams it, and ALWAYS persists a real-uuid assistant message + debits tokens; the Phase-8 LLM-error path now persists a fallback too. **R1.4** content-policy markers → `FALLBACK_TEXT`, never `""`; dropped the innocuous markers ("try rephrasing…") that were nuking genuine replies. **R1.5** tool-loop dead ends narrated (unknown tool → names it + close authorized matches; unauthorized → distinct message; MAX_TOOL_ROUNDS exhausted → "needs more steps than I can chain"; loop exception → error event + persisted fallback) — no more bare `break`. **R1.1/R1.2 (frontend)** the SSE switch gained an `error` handler + unknown-event `console.warn`; malformed SSE JSON warns instead of `catch {}`; an empty finalized message renders a fallback line instead of being filtered out; a post-await backstop covers silent resolves; the `done` dedupe no longer drops streamed content. Suite **1278→1290 passed / 0 failed / 12 deselected**; +12 new tests (`api/test_r1_turn_contract.py`, `unit/test_r1_llm_client.py`). Deferred (logged in `DEFERRED-AND-DISCOVERIES.md`): full single-exit token-debit consolidation for confirm/param early-returns; fallback-persist on the pre-turn (save-message/context-build) fatal paths; frontend event-switch jest test (→R8). Logs: `implementation-artifacts/ai-reliability/epic-R1-completed.md` + `epic-R1-review.md`. |

**Legend:** 📋 next · 🟢 in-progress · ✅ done · 🟦 queued · ⏸️ gated

## Per-Part BMad Cycle (Full Ceremony)

1. **Scope & document** — `bmad-document-project` on the part's surface area → `_bmad-output/parts/<part-slug>/scope.md`
2. **Architecture** — `bmad-create-architecture` if architectural changes are expected → `_bmad-output/parts/<part-slug>/architecture.md`
3. **Stories** — `bmad-create-epics-and-stories` → list of stories targeted at the part
4. **Readiness check** — `bmad-check-implementation-readiness` → gate before coding
5. **Per story** — `bmad-create-story` → `bmad-dev-story` → `bmad-qa-generate-e2e-tests` → `bmad-code-review`
6. **Retro** — `bmad-retrospective` → lessons captured for the next part
7. **Final adversarial pass** — `bmad-review-adversarial-general` on the whole part's diff

## Stale-context discipline

`_bmad-output/project-context.md` is the foundation that every BMad skill loads as persistent fact. **Refresh it before each Part kicks off** via `bmad-generate-project-context` — the codebase moves faster than the document.

## Decisions captured

- **Frontend Foundation is a separate part (#8)** — shared components audited once, not re-touched per vertical.
- **Per-part rigor: full ceremony** — slower but enterprise-grade.
- **Teacher and Student verticals are gated** on Story 7-39 (auth activation) regardless of sweep order.

## Final State (2026-05-16) — Quality Sweep COMPLETE

**Backend tests: 699 passing, 0 skipped.** Commit: `c7630f8`

**Branch isolation: 3 waves complete**
- Wave 1 (Part 4): `tool_functions_v2.py` — 35 callsites
- Wave 2 (Part 11 review): `issues.py` — 34 callsites  
- Wave 3 (Round 2 review): `operations.py` — 19 callsites (expenses/incidents/transport/assets)
- Remaining `scoped_filter` in `operations.py` are intentionally school-wide (leave_requests, announcements, users) with `# branch-scope: intentional` comments

**Platform integrity review results:** Party-mode + adversarial + edge-case + architecture + UX + code quality all applied across 2 rounds. 50+ bugs found and fixed. 40 new tests added.

**AI layer hardening:** LLM temp 0.2/0.7, max_tokens 1200, 4 new tools (timetable, exam analytics, upcoming events, parent message draft), Hindi content filter (16 patterns), it_tech/maintenance scope resolvers.

**Frontend fully wired:** All 6 missing endpoint connections added (class-summary, cost-summary, upcoming schedule, 202 discount-pending, pending-approvals panel, fee/my summary).

## Outstanding cross-cutting risks (track here as discovered)

- ~~Migration 014~~ **RESOLVED in Part 4**
- ~~`db.otps` dead collection~~ **RESOLVED in Part 4** — dropped via migration 018
- ALLOWED_ROLES override may unlock students past their YAML floor — tracked, low risk.
- `_normalize_fee_key` now uses `|` separator — safe. Old `:` separator could cause collision with fee_heads containing colons.
- `export_results` N+1 — **RESOLVED in Round 2 review**
- `issues.py`/`operations.py` branch isolation — **RESOLVED via 3-wave migration**

## Next Steps (Product Backlog)

1. **NEXT: Story 7-39** — Teacher/student login activation (gates all teacher+student features)
2. **6-34** — MongoDB Atlas M10 upgrade (production readiness)
3. **6-35** — Azure OpenAI India region + DPA sign-off (compliance)
4. Deploy → 4 clean pilot weeks → Phase 7 growth features
5. **7-40** WhatsApp/Twilio → **7-42** Token billing → **7-43** Health dashboard → **7-44** School onboarding → **7-45** SaaS multi-tenancy → **7-46** Google Maps transport
