# AI Layer Reliability — Deferred Items & Mid-Run Discoveries (running log)

One running file for the whole initiative (protocol rule 6). Every mid-run discovery not fixed in-run gets a row. Review at the START of every run; handle rows belonging to the current epic.

| Date | Epic | What was found | Status | Why deferred / where it gets fixed |
|------|------|----------------|--------|-------------------------------------|
| 2026-07-08 | (pre-run, audit) | Residual unaudited areas: `ai_rate_limiter.py`, `ai_metrics.py`, `ai_shadow_mode.py`, `actor_context.py`, `txn_context.py`, `idempotency.py`, `services/sse.py`, domain service write paths dispatched by AI tools | DEFERRED | Scheduled as story R11.6 (residual audit sweep) |
| 2026-07-08 | (pre-run, standing) | 25 pinned pre-existing test failures | DEFERRED | Standing directive: fix LAST, after all epics ship. NOTE: these no longer reproduce on current main — suite is fully green (1278→1290). Re-confirm at initiative close before declaring them fixed. |
| 2026-07-08 | R1 | Pre-turn fatal error paths (Phase-1 save-message `chat.py:~1567`, Phase-4 context-build `chat.py:~1684`) yield `error`+`done` WITHOUT persisting a fallback assistant message | DEFERRED | R1.3 AC3 fully covers the incident-critical LLM/Phase-14 paths; extending persistence to every early-return is a broader single-exit refactor. Fix in R8 (Frontend/…) or a dedicated chat.py single-exit story. |
| 2026-07-08 | R1 | Confirm / missing-param / resolution-error early returns don't route token debit through the single Phase-14 exit (R1.3 AC4 "every path") | DEFERRED | Risk: threading `record_usage` into ~4 mid-generator exits (tool_name may be unbound at some) on a LIVE chat path. Empty-turn undercount (the incident) IS fixed. Do the full single-exit consolidation as a focused story. |
| 2026-07-08 | R1 | Frontend event-switch jest unit test (R1.1 AC4) not written | DEFERRED | Requires a component-level render harness; behavior is covered by the backend turn-contract test + babel-parse. Land in R8 (Frontend Chat Resilience). |
| 2026-07-08 | R1 | R1.6 retry uses `RETRY_MAX_COMPLETION_TOKENS=8000`, not the literal 4000 in AC1 | FIXED (documented) | With default already 4000 (AC2), a 4000 retry is a no-op; 8000 makes the retry effective. Honours AC2 exactly + AC1 intent. |
| 2026-07-08 | R2 | Post-commit assistant-message persistence in `chat.py` could still 500 a *committed* plan (XM9 covered metric/audit, not the message write) | FIXED (in-run) | Small + safe; wrapped in try/except (log + continue). Same XM9 root cause, extended one line beyond the finding's literal scope. |
| 2026-07-08 | R2 | On the **no-op** session tier (dev only), a mid-plan step failure leaves earlier writes un-rolled-back while their idempotency claims are compensated → retry could double-write | DEFERRED | Dev-only: R2.3 confines the no-op session to `ENVIRONMENT == development`; production uses a real txn (full rollback). No production impact. |
| 2026-07-08 | R2 | AC3 (XM1) resolved by **retaining + documenting** the saga machinery rather than removing it | DECISION (documented) | Live AI-write side effects are Mongo notifications that enlist in the txn (roll back correctly); firing them post-commit would be wrong. Saga infra kept for a future genuine non-Mongo effect. See epic-R2-completed.md R2.5 AC3. |
| 2026-07-08 | R3 | `get_school_pulse` (registry owner/admin-only) was advertised to ALL teacher variants → guaranteed 403 | FIXED (in-run) | Same C4/parity class as the epic; small + safe. Removed from teacher tool lists; now caught permanently by the parity gate (assertion 2). |
| 2026-07-08 | R3 | `query_maintenance_requests` registry `sub_categories` widened to include `it_tech` (was `maintenance` only) | FIXED (in-run) | Aligns registry to the long-standing prompt intent (IT-tech reads the ticket queue); read-only tool, no write/confirm concern. |
| 2026-07-08 | R3 | Default admin context: sub_categories other than principal/accountant/transport_head/receptionist still fall through to **principal** context (`context_builder.py` ~543) — info over-exposure for it_tech/maintenance/management | RESOLVED (R5) | Fixed in R5: default admin now gets a minimal context (no school-wide operational data), aligned with scope_resolver deny-by-default. See epic-R5-completed.md. |
| 2026-07-08 | R5 | `tool_mark_attendance` branch-scoped the class *name* lookup (R5.2 AC2) but a *directly-supplied* `class_id` from another branch was not re-checked (`student_attendance` has no branch_id, so the service can't guard it) | FIXED (in-run) | Small + safe defense-in-depth: the resolved class_id is validated against a branch-scoped class lookup before the write. Extends R5.2 AC2's intent beyond the literal "name lookup" wording. |
| 2026-07-08 | R5 | HOD `subject` scope over non-student collections (e.g. `exam_results`) now returns the impossible filter instead of `{}` | DECISION (documented) | Fail-closed is the safe direction (X6 was fail-open). If HODs later need scoped exam access, that is a separate explicit feature, not a tenancy fix. |
| 2026-07-08 | R6 | Owner-memory erasure wired into `delete_staff` (staff **deactivation**), not a dedicated hard-erase endpoint | DECISION (documented) | No separate staff hard-erase endpoint exists; deactivation is the staff lifecycle-end and the correct DPDP trigger. Satisfies XM5 AC1. Revisit if a distinct staff DPDP-erase endpoint is added later. |
| 2026-07-08 | R6 | Full indexed/scored recall at scale (replace the `list_memories` `.to_list(2000)` scan with an index) | DEFERRED | Out of R6 scope. The per-owner cap (500) bounds the scan now; durable + indexed recall is R10.1 (gated). |
| 2026-07-08 | R6 | Vector `rebuild_index_from_mongo` uses a single `.to_list(100000)` read on startup | DECISION (documented) | Only runs when the vector path is ENABLED (default OFF) and per-instance is one school; 100k ceiling is ample at this scale. Paginate if/when vectors ship to a large tenant. |
| 2026-07-08 | R3 | `_ACCOUNTS_TOOLS` omits `query_fee_status`/`query_student_record` though the registry authorizes accountant | DEFERRED | Nice-to-have, not a bug (accountant has record_fee_payment + fee reads; passes parity gate 4a as an intentional omission). Consider advertising in a future UX pass. |
| 2026-07-08 | R3 | Parity gate assertion 4 relaxed from per-role exhaustive coverage to global-coverage + MUST_ADVERTISE_EVERYWHERE | DECISION (documented) | ~400 authorized-but-unadvertised pairs are intentional prompt curation; a literal allowlist would be brittle noise. See epic-R3-completed.md design note. |
| 2026-07-08 | R11.1 | Credentialed LLM-judge baseline (`scores-baseline.json`) not yet written | DEFERRED (needs creds) | This dev/test environment has no Azure OpenAI credentials (tests use fakes). Baseline is established on the first `pytest -m llm_eval` run in credentialed nightly/pre-release CI. Always-on structural + judge-logic tiers are green now. STEP 4e comparisons begin from R4. |
| 2026-07-08 | R11.1 | R11.4 will expand Hinglish coverage to ≥8 (corpus currently has 6) | DEFERRED | R11.1 AC1 seeds Hinglish; R11.4 AC2 is the ≥8 requirement + Devanagari. Add the extra entries in R11.4. |
| 2026-07-08 | R4 | `get_transport_status` inline driver-phone mask exposed 7 digits (weaker than canonical) — not a named finding | FIXED (in-run) | Small + safe DPDP improvement aligned with R4.4 AC3; switched to canonical `_mask_phone`. |
| 2026-07-08 | R4 | Envelope-shape test invokes tools on an EMPTY DB, so a success-path-only `denied` omission can slip through | FIXED (in-run) + noted | Found + fixed `get_announcements` success path; audited all manual-envelope success returns. A future enhancement could seed per-tool data to exercise success paths in the shape gate. |
| 2026-07-08 | R4 | L2 (mixed naive/UTC audit timestamps, `v2:1203`) not addressed | FIXED (R7) | Root fix: `_audit_doc` (`tool_functions_v2.py`) and `audit_service._normalise_doc` now stamp `datetime.now(timezone.utc).isoformat()`, matching `actor_ctx.now()`/`ai_metrics`. Audit log is no longer a naive/UTC mix. |
| 2026-07-08 | R7 | Extra N+1 storms beyond the audit's named lines: `tool_get_staff_list` (per-staff attendance find), `tool_get_leave_requests` (per-leave staff find_one), `tool_get_class_list` (per-class teacher+count), `tool_get_student_council` (per-member student/class/house) | FIXED (in-run) | Same M4 class as the epic; all batched with `$in`/aggregate. Small + safe. |
| 2026-07-08 | R7 | Test double gaps: `FakeCollection` has no `.distinct` and its `.aggregate` `$group` cannot key on a compound `_id` dict | FIXED (in-run, product-side) | Avoided both in product code: context_builder defaulter count uses find+set (not `.distinct`); `get_house_standings` groups in Python (not a compound-`_id` aggregate). No conftest change needed. Worth adding `.distinct` to the fake in a future test-infra pass. |
| 2026-07-08 | R7 | `tool_get_fee_summary`/`context_metrics` fee math was already unified in AI-Hardening; M5's remaining divergence was `fee_defaulters` (v2, overdue-only), smart-alerts collection-rate, and context_builder outstanding (pending+overdue only, no unpaid/partial) | FIXED (R7.1) | All four now route through `ai/fee_metrics.py`. Defaulter = any student with outstanding balance (overdue/pending/unpaid/partial). |
| 2026-07-10 | R5 (post-merge review) | `_parse_custom_range` on a reversed/degenerate coordinator range ("5-1") built an empty alternation `^()\b` that matched EVERY class → widened a coordinator to the whole branch (the exact X6/L6 leak class; escaped AC2's "zero classes → impossible filter" because the DB returned everything) | **FIXED (in-run)** | Med severity. Guard: empty/reversed range → `return []` (deny by default). Regression: `test_reversed_coordinator_range_resolves_to_no_classes`. |
| 2026-07-10 | R5 (post-merge review) | `context_builder` admin dispatch used exact case-sensitive `sub_category == "accountant"` while `scope_resolver` normalizes + aliases (`"accounts"`, `"Accountant"`, `"transport"`, `"front_desk"`); R5 worsened this — aliased/mis-cased admins that the resolver grants tool access now fell to the minimal chat context (under-informed) | **FIXED (in-run)** | Low. Normalized + alias-mapped the dispatch to mirror `_resolve_admin_scope`. Regression: `test_miscased_accountant_gets_accounts_context_not_minimal` + `test_unknown_admin_subcategory_still_minimal`. |
| 2026-07-10 | R5 (post-merge review) | KG in-charge class regex `^(KG|Nursery|LKG|UKG)` unanchored (matched "KGeography"), inconsistent with the coordinator-range `\b`-anchoring (AC4) | **FIXED (in-run)** | Low. Added `\b`. Regression: `test_kg_incharge_regex_excludes_non_kg_class_names`. |
| 2026-07-10 | R6 (post-merge review) | `_INLINE_NOTE_RE` matched bare `note down`/`jot down + <domain object>` ("note down attendance for class 5") → swallowed pre-LLM as a memory save (narrow re-opening of the X3 hijack) | **FIXED (in-run)** | Low. `jot/note down` now require a memory connector (`that`/`about`); explicit note-noun phrasings unchanged. Regression: `test_note_down_domain_object_falls_through` + `test_note_down_with_memory_connector_still_saves`. |
| 2026-07-10 | R6 (post-merge review) | Degraded keyword-only recall logged at `logger.debug` under an INFO root → still invisible in prod, so XM10's "no longer silent" goal wasn't met | **FIXED (in-run)** | Low. WARN when `vector_enabled()` but index down (real degradation); debug when vectors intentionally off (default) to avoid per-turn spam. Regression: `test_degraded_recall_warns_only_when_vectors_enabled`. |
| 2026-07-10 | R7 (post-merge review) | `tool_get_fee_defaulters` reported negative `days_overdue` for a defaulter surfaced via a future-dated `pending` invoice (AC3 widened defaulter to include pending) | **FIXED (in-run)** | Low. `max(0, …)` clamp. Regression: `test_future_dated_pending_defaulter_days_overdue_clamped`. |
| 2026-07-10 | R7 (post-merge review) | `tool_get_upcoming_events` window does a lexicographic string compare on `event_date`, stored verbatim; a non-ISO date ("15/07/2026") was silently dropped/mis-ordered | **FIXED (in-run)** | Low. New `_normalize_iso_date` coerces to ISO on write (dd/mm/yyyy etc.), unparseable → dateless (falls back to sent_at). Regression: `test_normalize_iso_date_coerces_and_rejects`. |
| 2026-07-10 | R7 (post-merge review) | `tool_get_exam_results_summary` reported `students=len(marks)` but computed `pass_rate` over `len(scorable)` — the two disagree when some rows lack a known max, making the subset rate read as if it covered all students | **FIXED (in-run)** | Low. Pass-rate is `"N/A"` unless every graded student is scorable. Regression: `test_exam_pass_rate_na_when_partial_scorable`. |
| 2026-07-10 | R6 (post-merge review) | `_paged_find` has no `.sort()`, so page order relies on natural order (theoretically unstable under concurrent writes) | NOTED (accepted) | Each sweep reads fully before deleting and `erase_owner_memories` also runs an authoritative `delete_many(q)`, so no rows are actually lost. Optional `.sort("_id", 1)` hardening if a large tenant ever ships. |
| 2026-07-10 | R8 | Client terminal backstop dropped the finalized reply when every SSE frame (incl. `done`) arrived in ONE React batch — `streaming` never committed `true`, so the streaming-transition flush effect saw no change | **FIXED (in-run)** | Latent silent-empty-turn (the incident class). Post-`await` backstop now flushes `pendingFinalMsgRef` directly (dedupe by id). Regression: `ChatInterface.r8` happy-path synchronous-mock test. |
| 2026-07-10 | R8 | R8.4 AC3 sanctioned "allow the renderer's own style attrs via sanitizer config" — but DOMPurify does NOT neutralize `url(javascript:…)` in a `style` value under jsdom (caught by test) | DECISION (documented) | Chose the SAFER sanctioned option instead: emit bare tags + rely on existing `.prose-chat` CSS; sanitizer stays strict (strips style/class/handlers). Minor cosmetic loss: markdown-table ₹ amounts no longer tinted yellow and wide tables lack an inline overflow-scroll wrapper (structured RichDataTable is unaffected). |
| 2026-07-10 | R8 | `executeAction` used a raw `fetch` (no 401 refresh) — an expired token silently failed a confirm-card action | FIXED (in-run) | Routed through `apiFetch`; same FL class as R8.4. Small + safe. |
| 2026-07-10 | R8 | R1 backend early-return paths (Phase-1 save-message, Phase-4 context-build) yield `error`+`done` WITHOUT persisting a fallback assistant message — the row said "fix in R8 (Frontend) OR a dedicated chat.py single-exit story" | DEFERRED (decision) | R8's stories are all frontend; this is a backend single-exit refactor on a live path. The strengthened **client** backstop (R8) now renders a visible fallback for any residual backend bare-return, so the user-facing symptom is covered. Do the backend persistence consolidation as a focused chat.py story (candidate for R9 or a dedicated story). |
| 2026-07-10 | R8 | `LayoutRouting.test.js` (2 tests) throws an opaque `AggregateError` on full-Harness render | DEFERRED (pre-existing) | Confirmed failing on `main` before R8 (baseline stash run). A global `scrollIntoView` polyfill was added this run (needed for chat-render tests) but did NOT fix it — separate jsdom/React-19 harness gap. Treat like the pinned baseline failures; fix in a test-infra pass, not mid-epic. |
