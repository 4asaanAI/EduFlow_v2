You are activating the BMAD skill: bmad-distillator

- skill-root: _bmad/core/bmad-distillator
- project-root: (current working directory)

Read the full skill definition from `_bmad/core/bmad-distillator/SKILL.md` and follow it exactly, substituting:
- `{skill-root}` → `_bmad/core/bmad-distillator`
- `{project-root}` → the project working directory
- `{skill-name}` → `bmad-distillator`
